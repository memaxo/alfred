# Default Postgres URL for integration tests. Override with DATABASE_URL=sqlite::memory:
# when you want lightweight in-memory tests without a Postgres service.
DATABASE_URL=postgresql://alfred:alfred@localhost:5432/alfred_test
VITE_TEST_MODE=true
