# failtest fixture

This is a tiny Bun/TypeScript project for Harbor evaluation.

- `bun test` currently fails.
- The intended fix is to correct the implementation in `src/math.ts` without modifying tests.
