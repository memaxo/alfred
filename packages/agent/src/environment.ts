/**
 * Convenience re-export for workspace/environment utilities.
 *
 * Allows imports like `@alfred/agent/environment` to resolve to the canonical
 * `src/environment/index.ts` surface.
 */
export * from "./environment/index";
