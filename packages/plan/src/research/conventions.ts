import type { Convention } from "./types.js";

/**
 * Extract project-specific conventions
 */
export async function extractConventions(
  _projectId?: string
): Promise<Convention[]> {
  // STUB: Returns empty array initially
  // Will be implemented in Phase 4 (P4-6)
  return [];
}
