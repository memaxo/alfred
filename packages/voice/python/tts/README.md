# TTS Module (Maya1)

This module implements the TTS server using the Maya1 model and SNAC decoding.

## Usage

```bash
python -m tts
```

## Protocol

See `packages/voice/src/process/ipc.ts` for the JSON-IPC protocol definition.
